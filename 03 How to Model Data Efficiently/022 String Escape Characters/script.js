let str = "he said, \"I want pasta + nasta.\""; // adding quotes in string
console.log(str);

let str2 = "hello!\ngoodnight"; // prints a new line
console.log(str2);

// "\", it will cause error. In order to show backslash in string, do it like:
let str3 = "\\";
console.log(str3);